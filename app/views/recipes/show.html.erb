<div>
  <% if flash[:notice] %>
    <p class="alert alert-success" role="alert"><%= notice %></p>
  <% end %>
  <table class="table">
    <%= render partial: "recipe_header" %>
    <tbody>
      <%= render @recipe %>
      <td class="d-flex gap-2"><%= link_to "Edit", edit_recipe_path(@recipe), class: "btn btn-outline-primary btn-sm" %>  <%= button_to "Destroy", @recipe, method: :delete, class: "btn btn-outline-danger btn-sm small-btn"%></td>
    </tbody>
  </table>
  <div>
    <%= link_to "🔙 Back to recipes", recipes_path, class: "btn btn-outline-secondary btn-sm"%>
  </div>
</div>
